services:
  compta-tva-app:
    build: .
    container_name: compta-tva-app
    ports:
      - "3030:80"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
